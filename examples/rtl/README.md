# RTL Example

The repository includes a bidi plugin scaffold for the roadmap phase where full UAX #9 handling lands. The current core stores per-run direction metadata so mixed-direction content can already be modeled at the run layer.
