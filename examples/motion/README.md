# Motion Example

Combine the motion plugin with normal edits or width changes:

- create the plugin with `createMotionPlugin()`
- install it through `ByeText.create({ plugins: [...] })`
- inspect active animations through `doc.getPlugin('motion')`
