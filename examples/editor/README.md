# Editor Example

Use the document edit APIs to build an editor shell:

- map keystrokes to `insert()` and `delete()`
- use `charToPosition()` and `positionToChar()` for cursor and hit-testing
- layer the selection plugin when you need persistent selection state
